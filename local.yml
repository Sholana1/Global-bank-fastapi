services:
  postgres:
    build:
      context: .
      dockerfile: ./backend/docker/local/postgres/Dockerfile
    ports:
      - "5432:5432"
    volumes:
      - globalbank_data:/var/lib/postgresql/data
    env_file:
      - ./.envs/.env.local
    networks:
      - globalgen_local_nw

networks:
  globalgen_local_nw:
    external: true

volumes:
  globalbank_data: